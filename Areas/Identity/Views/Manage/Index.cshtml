@using App.Areas.Identity.Models.Manage.IndexViewModel;
@model IndexViewModel;
@{
    ViewData["Title"] = "Quản lý tài khoản của bạn";
}

<h1>@ViewData["Title"].</h1>
<p class="text-success">@ViewData["StatusMessage"]</p>

<div>
    <h4>Thông tin tài khoản</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-4">Thông tin cơ bản:</dt>
        <dd class="col-sm-6"> 

            <dl class="row">
                 <dt class = "col-sm-6">
                    @Html.DisplayNameFor(m => Model.Id)
                 </dt>   
                 <dd class = "col-sm-6">
                    @Html.DisplayFor(m => Model.Id)
                 </dd>
            </dl>
            <dl class="row">
                 <dt class = "col-sm-6">
                    @Html.DisplayNameFor(m => Model.UserName)
                 </dt>   
                 <dd class = "col-sm-6">
                    @Html.DisplayFor(m => Model.UserName)
                 </dd>
            </dl>
             <dl class="row">
                 <dt class = "col-sm-6">
                    @Html.DisplayNameFor(m => Model.Email)
                 </dt>   
                 <dd class = "col-sm-6">
                    @Html.DisplayFor(m => Model.Email)
                 </dd>
            </dl>
              <dl class="row">
                 <dt class = "col-sm-6">
                    @Html.DisplayNameFor(m => Model.PhoneNumber)
                 </dt>   
                 <dd class = "col-sm-6">
                    @Html.DisplayFor(m => Model.PhoneNumber)
                 </dd>
            </dl>
               <dl class="row">
                 <dt class = "col-sm-6">
                    @Html.DisplayNameFor(m => Model.HomeAdress)
                 </dt>   
                 <dd class = "col-sm-6">
                    @Html.DisplayFor(m => Model.HomeAdress)
                 </dd>
            </dl>                   
                <dl class="row">
                 <dt class = "col-sm-6">
                    @Html.DisplayNameFor(m => Model.Age)
                 </dt>   
                 <dd class = "col-sm-6">
                    @Html.DisplayFor(m => Model.Age)
                 </dd>
            </dl>      
            <a asp-action="EditProfile" class="btn btn-primary btn-sm">Sửa</a>
 
        </dd>
    </dl>
</div>